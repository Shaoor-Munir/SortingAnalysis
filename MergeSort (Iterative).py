def merge(A, right, middle, left):





def merge_sort (A):
    current_size = 1
    starting_index = 0

    length = len(A)

    while current_size <= length-1:
        starting_index = 0

        while starting_index < length-1:
